import { SpaceToHypenPipe } from './space-to-hypen.pipe';

describe('SpaceToHypenPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToHypenPipe();
    expect(pipe).toBeTruthy();
  });
});
